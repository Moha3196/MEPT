AddType application/x-httpd-php-latest .php
RewriteEngine On
RewriteCond %{HTTPS} !on
RewriteRule .? https://%{HTTP_HOST}%{REQUEST_URI} [R=302,L]
# RewriteRule .? https://testing.rosenoern-productions.dk [R=301,L]
